server:
  port: 9494

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/liumapp/spring-cloud-config-demo
          search-paths: conf

